if (true) { code; } 
else { code; }


if (cond) {
    stuff;
  } /* comment */ else if (cond) {
    stuff;
  }
  // comment
  else {
    stuff;
  }

for (;;) {
    code;
    /* comment
    comment c
    comment 
    */
    if (cond) {
        code;
    }
}